  <div class="jumbotron">
      <h1 class="display-4">Bem Vindo ao blog NODE</h1>
      <p class="lead">dsssss</p>
      <hr class="my-4">
      <p>iuueeeuuusss</p>
      <p class="lead">
          <a href="" class="btn btn-primary btn-lg" role="button">Criar Conta</a>
      </p>
  </div>
<h2>Postagens Recentes</h2>

{{#each postagens}}
  <div class="card mt-3 pt-2">
      <div class="card-body">
          <h1>{{titulo}}</h1>
          <p>{{descricao}}</p>
          <a href="/postagem/{{slug}}"><button class="btn btn-success">Leia mais</button></a>
          <small>{{categoria.nome}}</small><br>
          <small>data de Publicaçao: {{data}}</small>
          <hr>
    </div>
  </div>
  {{else}}
     <h5>Nenhuma Postagem</h5>
  {{/each}}
     <div class="p-2"></div>