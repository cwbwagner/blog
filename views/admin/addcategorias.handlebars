{{#each erros}}
    <div class="alert alert-danger">{{text}}</div>
{{else}}

{{/each}}
<h5>Nova Categoria:</h5>
<div class="card">
    <div class="card-body">
  <form action="/admin/categorias/nova" method="post">
   <label for="nome">Nome:</label>
   <input type="text" id="nome" name="nome" class="form-control" placeholder="Nome da categoia">
   <label for="nome">Slug:</label>
   <input type="text" id="slug" name="slug" class="form-control" placeholder="Slug da categoria">
   <button type="submit" class="btn btn-success mt-3">Criar Categoria</button>
</form>
  </div>
</div>